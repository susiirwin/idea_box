<h1>Admin Categories</h1>

<% @categories.each do |category| %>
  <%= link_to category.topic, admin_category_path(category.id) %>
<% end %>

<p>
  <%= link_to "Create a New Category", new_admin_category_path %>
</p>

<p>
  <%= link_to "See All Categories", admin_categories_path %>
</p>

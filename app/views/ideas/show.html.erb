<h2>You have a GREAT idea: <%= @idea.title %></h2>

<ul>
  <li>Thought: <%= @idea.thought %></li>
  <% if @idea.category %>
    <li>Topic: <%= @idea.category.topic %></li>
  <% end %>
</ul>

<%= link_to "Create Idea", new_user_idea_path(current_user) %>
<%= link_to "View all Ideas", user_ideas_path(current_user.id) %>
